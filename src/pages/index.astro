---
export const prerender = false;

type MaybeUser = { id: string; email: string | null } | undefined;

const user = Astro.locals.user as MaybeUser;
const destination = user ? "/in-progress" : "/auth";

return Astro.redirect(destination);
---
